import store from "@/store";
import { SET_ACCESS_LEVEL } from "@/store/mutation-types.js";

export default {
  methods: {
/*
    // debugging helper method
    changeAccess () {
      const al = store.getters.accessLevel; 
      store.commit( SET_ACCESS_LEVEL, ( al == 0 ) ? 6 : al - 1  );
    },
*/
  }
}
